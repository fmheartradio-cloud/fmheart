name = "fmheart-radio-proxy"
main = "radio-stream-worker.js"
compatibility_date = "2024-11-01"

# Free plan works for light traffic; Paid ($5/mo) recommended for many concurrent listeners.
# Cloudflare does not charge egress bandwidth.
