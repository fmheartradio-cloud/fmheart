name = "fmheart-radio-proxy"
main = "radio-stream-worker.js"
compatibility_date = "2024-11-01"
workers_dev = true

# Serve on the existing fmheart.lk Cloudflare zone (zero egress).
[[routes]]
pattern = "stream.fmheart.lk"
custom_domain = true
